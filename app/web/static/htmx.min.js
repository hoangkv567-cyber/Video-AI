/*
 * PLACEHOLDER — vendor the real htmx before production use.
 *
 * Download htmx 2.x locally (no CDN at runtime or in tests):
 *   https://unpkg.com/htmx.org@2/dist/htmx.min.js
 * and replace this file with it, keeping the same path so
 * <script src="/static/htmx.min.js"> in base.html stays valid.
 *
 * This stub only prevents console errors while offline; hx-* attributes are
 * inert until the real library is vendored.
 */
window.htmx = window.htmx || {
  version: "0.0.0-placeholder",
  process: function () {},
  trigger: function () {},
  on: function () {},
  ajax: function () {},
};
